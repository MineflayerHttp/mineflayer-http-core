{
  "name": "mineflayerhttp",
  "version": "1.0.0",
  "description": "http based mineflayer",
  "main": "src/index.js",
  "scripts": {
    "clean": "rmdir /s /q bin && rmdir /s /q logs",
    "build": "npm install && tsc --listFiles >nul 2>&1",
    "start": "npm run build && node bin/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "CollidaCube",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "mineflayer": "^4.8.1",
    "prompt-sync": "^4.2.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.3.1",
    "@types/prompt-sync": "^4.2.0",
    "@types/uuid": "^9.0.2"
  }
}
